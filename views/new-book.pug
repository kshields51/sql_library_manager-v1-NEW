extends layout

block content


    h1 New Book

    include form-error

        form(action="/books/new", method="post")
            p
                label(for='title') Title
                input#title(name='title', type='text', value=book.title)
            p
                label(for='author') Author
                input#author(name='author', type='text', value=book.author)
            p
                label(for='genre') Genre
                input#genre(name='genre', type='text', value=book.genre)
            p
                label(for='year') Year
                input#year(name='year', type='text', value=book.year)
            p
                input(type='submit', value='Create New Book')
            p
                a.button(href='/books/') Cancel


    //This is the form that allows users to add a new book to the database. If the user forgets to include a title or an author the form-error template is dynamically rendered in. 
